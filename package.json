{
  "name": "scientific-memory",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm --dir portal build",
    "dev": "pnpm --dir portal dev",
    "lint": "pnpm --dir portal lint",
    "format": "pnpm --dir portal format",
    "portal:test": "pnpm --dir portal test"
  }
}
